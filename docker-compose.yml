version: '3'

services:
  learning_go:
    build: ./src/learning_go
    volumes:
      - ./src/learning_go/app:/app
  
  mux:
    build: ./src/mux
    volumes:
      - ./src/mux/go:/go
    ports:
      - 5000:5000
